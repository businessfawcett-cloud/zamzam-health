import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { SanityLive } from "@/sanity/lib/live";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Northside Primary Care – Compassionate Primary Care in Chicago",
  description:
    "Dr. Sarah Mitchell provides compassionate, patient-centered primary care in Chicago's Lakeview neighborhood. Schedule an appointment today.",
  openGraph: {
    title: "Northside Primary Care – Compassionate Primary Care in Chicago",
    description:
      "Dr. Sarah Mitchell provides compassionate, patient-centered primary care in Chicago's Lakeview neighborhood.",
    type: "website",
    locale: "en_US",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        {children}
        <SanityLive />
      </body>
    </html>
  );
}
